import { LoginForm } from 'components';

import { Main } from './Login.styled';

const Login = () => {
  return (
    <Main>
      <title>Log in</title>
      <LoginForm />
    </Main>
  );
};

export default Login;
